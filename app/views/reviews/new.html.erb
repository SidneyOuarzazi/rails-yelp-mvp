<h1>Please write your review for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, :collection => (0..5).to_a  %>
  <%= f.input :content %>
  <%= f.submit "Create", class: "btn btn-primary" %>
<% end %>
